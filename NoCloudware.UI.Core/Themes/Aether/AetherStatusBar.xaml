<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style x:Key="StatusBarStyle" TargetType="Border">
        <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
        <Setter Property="CornerRadius" Value="8"/>
        <Setter Property="Padding" Value="10,6"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
    </Style>

    <Style x:Key="StatusCounterStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="Margin" Value="6,0"/>
    </Style>

    <Style x:Key="StatusCounterPositiveStyle" TargetType="TextBlock"
           BasedOn="{StaticResource StatusCounterStyle}">
        <Setter Property="Foreground" Value="{DynamicResource StatusProcessedBrush}"/>
    </Style>

    <Style x:Key="StatusCounterErrorStyle" TargetType="TextBlock"
           BasedOn="{StaticResource StatusCounterStyle}">
        <Setter Property="Foreground" Value="{DynamicResource StatusErrorBrush}"/>
    </Style>

    <Style x:Key="StatusCounterDefaultStyle" TargetType="TextBlock"
           BasedOn="{StaticResource StatusCounterStyle}">
        <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}"/>
    </Style>

</ResourceDictionary>
