<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style x:Key="AetherFileListBoxStyle" TargetType="ListBox">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
        <Setter Property="AllowDrop" Value="True"/>
    </Style>

    <Style x:Key="AetherFileItemStyle" TargetType="ListBoxItem">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
        <Setter Property="Padding" Value="4,2"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
    </Style>

    <DataTemplate x:Key="AetherFileItemTemplate">
        <StackPanel Orientation="Horizontal" Margin="0,3">
            <TextBlock Text="{Binding FileName}"
                       Foreground="{DynamicResource TextPrimaryBrush}"
                       FontSize="13"/>
            <TextBlock Text="{Binding StatusText}"
                       Foreground="{Binding StatusColor}"
                       FontWeight="SemiBold"
                       Margin="8,0,0,0"/>
        </StackPanel>
    </DataTemplate>

    <!-- Colors for file status indicators -->
    <SolidColorBrush x:Key="StatusProcessedBrush" Color="#4CAF50"/>
    <SolidColorBrush x:Key="StatusErrorBrush" Color="#E81123"/>
    <SolidColorBrush x:Key="StatusPendingBrush" Color="#F0C040"/>
    <SolidColorBrush x:Key="StatusProcessingBrush" Color="#4A9EFF"/>

</ResourceDictionary>
